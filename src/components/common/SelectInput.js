import React, {PropTypes} from 'react';

const SelectInput = ({name, lable, onChange, defaultOptions, value, error, options}) => {
	return (
		<div></div>
	);
};

SelectInput.propTypes = {
	//myProp: PropTypes.array.isRequired
};

export default SelectInput;
